# Pinned: Material for MkDocs has announced that MkDocs 2.0 removes the plugin
# system with no migration path, so an unpinned build would break without
# warning.
mkdocs-material>=9.7.7,<10
mkdocs>=1.6,<2
